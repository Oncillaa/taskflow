services:
  app:
    build: .
    container_name: taskflow
    restart: always
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./:/taskflow
      - ./data:/taskflow/data
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload